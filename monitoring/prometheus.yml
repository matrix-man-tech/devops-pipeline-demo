global:
  scrape_interval: 15s      # Collect metrics every 15 seconds
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'devops-demo-app'
    static_configs:
      - targets: ['app:3000']   # 'app' = the service name in docker-compose
    metrics_path: '/metrics'